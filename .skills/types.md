# Types — dt-dev-platform

Fichier : `lib/types.ts`

## Contenu

- `ApiResponse<T>` — aligné `{ ok, data }`
- `ApiClientProfile`
- API key entity types
- Payment method types si présents

## Rules

Typer nouvelles méthodes `api.*` ici avant usage pages

## dt-api source of truth

Swagger `/api/docs/dtmoney-api` — valider types si drift
