# UI Components — dt-dev-platform

## Primitives

`components/ui/` — `button`, `card`, `input`, etc.

## Styling

- Tailwind 4 dark slate theme
- `cn()` utility pour merge classes
- `app/globals.css` — base Tailwind

## Layout marketing

`components/layout/` — nav marketing vs dashboard

## Rules

- `'use client'` sur pages dashboard interactives
- French UI copy
- Pas de lib UI lourde type MUI

## Next 16

Vérifier APIs deprecated avant d'ajouter patterns Next 15 training data
